{
  "name": "radar-ofertas-ml",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de inteligência, monitoramento e automação de ofertas do Mercado Livre",
  "main": "index.js",
  "scripts": {
    "build:packages": "npm run build --workspace=packages/domain && npm run build --workspace=packages/deal-engine && npm run build --workspace=packages/marketplace && npm run build --workspace=packages/messaging",
    "build:apps": "npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "build": "npm run build:packages && npm run build:apps",
    "package:extension": "mkdir -p dist && zip -r dist/radar-ofertas-ml-extension.zip apps/extension -x '*/.*' -x 'apps/extension/store-listing/*'",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev": "npm run dev:api"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
